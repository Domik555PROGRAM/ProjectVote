<Window x:Class="Project_Vote.UserPollsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_Vote"
        mc:Ignorable="d"
        Title="Ваши опросы" Height="450" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#f0f0f0">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Список ваших опросов" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>

        <ListView Grid.Row="1" x:Name="PollsListView" BorderThickness="1" BorderBrush="#ccc" Background="White">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Заголовок" Width="300" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="Дата создания" Width="150" DisplayMemberBinding="{Binding CreatedAt, StringFormat='dd.MM.yyyy HH:mm'}"/>
                    <GridViewColumn Header="Действия" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Изменить" Margin="2" Padding="10,5" Click="EditPoll_Click" Tag="{Binding Id}"/>
                                    <Button Content="Пройти тест" Margin="2" Padding="10,5" Click="RunPoll_Click" Tag="{Binding Id}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="#e6f2ff"/>
                                                <Setter Property="BorderBrush" Value="#99ccff"/>
                                                <Setter Property="Foreground" Value="#0066cc"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#cce6ff"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <Button Content="Удалить" Margin="2" Padding="10,5" Click="DeletePoll_Click" Tag="{Binding Id}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="#ffcccc"/>
                                                <Setter Property="BorderBrush" Value="#ff6666"/>
                                                <Setter Property="Foreground" Value="#cc0000"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#ff9999"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Grid.Row="2" Content="Закрыть" HorizontalAlignment="Right" Margin="0,15,0,0" Padding="15,5" Click="CloseButton_Click"/>
    </Grid>
</Window> 
