<Window x:Class="Project_Vote.Resultat_test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_Vote"
        mc:Ignorable="d"
        Title="Результаты тестов" Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#f8f8f8">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#357ABD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2A5D94"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="#ccc"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Результаты прохождения тестов" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <!-- Фильтры -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Тест:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Grid.Column="1" x:Name="TestsComboBox" 
                      SelectionChanged="TestsComboBox_SelectionChanged" Margin="0,0,15,0"/>
            
            <TextBlock Grid.Column="2" Text="Пользователь:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Grid.Column="3" x:Name="UsersComboBox" 
                      SelectionChanged="UsersComboBox_SelectionChanged" Margin="0,0,15,0"/>
            
            <Button Grid.Column="4" Content="Сбросить фильтры" 
                    Style="{StaticResource ActionButton}"
                    Click="ResetFilters_Click"/>
        </Grid>
        
        <!-- Список результатов -->
        <DataGrid Grid.Row="2" x:Name="ResultsDataGrid" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Background="White" BorderBrush="#ddd" BorderThickness="1"
                  RowBackground="White" AlternatingRowBackground="#f9f9f9"
                  CanUserSortColumns="True" 
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  SelectionChanged="ResultsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Название теста" Binding="{Binding TestTitle}" Width="*"/>
                <DataGridTextColumn Header="Пользователь" Binding="{Binding UserName}" Width="150"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Время" Binding="{Binding Time, StringFormat=HH:mm:ss}" Width="100"/>
                <DataGridTextColumn Header="Баллы" Binding="{Binding Score}" Width="90"/>
                <DataGridTextColumn Header="Результат" Binding="{Binding ScorePercent, StringFormat={}{0}%}" Width="90"/>
                <DataGridTemplateColumn Header="Действия" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Детали" Style="{StaticResource ActionButton}"
                                    Click="ViewDetails_Click" Tag="{Binding Id}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Заголовок детальной информации -->
        <TextBlock Grid.Row="3" Text="Детали прохождения" 
                   FontSize="20" FontWeight="Bold" Margin="0,15,0,10"/>
        
        <!-- Детальная информация об ответах выбранного результата -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <DataGrid Grid.Column="0" x:Name="AnswersDataGrid" 
                      AutoGenerateColumns="False" IsReadOnly="True"
                      Background="White" BorderBrush="#ddd" BorderThickness="1"
                      RowBackground="White" AlternatingRowBackground="#f9f9f9"
                      Margin="0,-25,10,25"
                      HeadersVisibility="Column"
                      CanUserSortColumns="True"
                      SelectionMode="Single"/>
            
            <Border Grid.Column="1" Background="White" BorderBrush="#ddd" BorderThickness="1" 
                    CornerRadius="5" Padding="15"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Информация о прохождении" 
                               FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                    
                    <StackPanel Grid.Row="1">
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Тест:" FontWeight="SemiBold" Margin="0,3"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="DetailTestName" Text="-" Margin="0,3" TextWrapping="Wrap"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Пользователь:" FontWeight="SemiBold" Margin="0,3"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="DetailUserName" Text="-" Margin="0,3"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Дата и время:" FontWeight="SemiBold" Margin="0,3"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="DetailDateTime" Text="-" Margin="0,3"/>
                            
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Длительность:" FontWeight="SemiBold" Margin="0,3"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="DetailDuration" Text="-" Margin="0,3"/>
                            
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Правильных:" FontWeight="SemiBold" Margin="0,3"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" x:Name="DetailCorrectCount" Text="-" Margin="0,3"/>
                            
                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Всего вопросов:" FontWeight="SemiBold" Margin="0,3"/>
                            <TextBlock Grid.Row="5" Grid.Column="1" x:Name="DetailTotalCount" Text="-" Margin="0,3"/>
                            
                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Результат:" FontWeight="SemiBold" Margin="0,3"/>
                            <TextBlock Grid.Row="6" Grid.Column="1" x:Name="DetailScore" Text="-" Margin="0,3"/>
                            
                            <TextBlock Grid.Row="7" Grid.Column="0" Text="Статус:" FontWeight="SemiBold" Margin="0,3"/>
                            <TextBlock Grid.Row="7" Grid.Column="1" x:Name="DetailStatus" Text="-" Margin="0,3"/>
                        </Grid>
                    </StackPanel>
                    
                    <!-- Визуализация результата -->
                    <Canvas Grid.Row="2" x:Name="ResultCanvas" Height="180" Width="180" 
                            HorizontalAlignment="Center" Margin="0,15">
                        <Ellipse Width="180" Height="180" Fill="#f0f0f0"/>
                        <Path x:Name="ResultArc" Fill="#4CAF50"/>
                        <Ellipse Width="140" Height="140" Canvas.Left="20" Canvas.Top="20" Fill="White"/>
                        <TextBlock x:Name="PercentText" Text="0%" FontSize="36" FontWeight="Bold" 
                                   Foreground="#333" Canvas.Left="62" Canvas.Top="70"/>
                    </Canvas>
                    
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Закрыть детали" Margin="5" 
                                Style="{StaticResource ActionButton}"
                                Click="CloseDetails_Click"/>
                        <Button Content="Пройти тест" Margin="5" 
                                Style="{StaticResource ActionButton}"
                                x:Name="TakeTestButton"
                                Click="TakeTest_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
