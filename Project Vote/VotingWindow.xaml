<Window x:Class="Project_Vote.VotingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_Vote"
        mc:Ignorable="d"
        Title="Голосование" Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:StringNotEmptyConverter x:Key="StringNotEmptyConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleTextBlock" 
                   Text="Название голосования" 
                   FontSize="24" FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" x:Name="DescriptionTextBlock" 
                   Text="Описание голосования" 
                   TextWrapping="Wrap" 
                   Margin="0,0,0,15"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="CandidatesItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" BorderBrush="#ddd" BorderThickness="1" 
                                CornerRadius="8" Margin="0,0,0,15" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Изображение кандидата -->
                                <Border Grid.Column="0" Width="150" Height="150" 
                                        Margin="0,0,15,0" BorderBrush="#eee" 
                                        BorderThickness="1" CornerRadius="4"
                                        Visibility="{Binding HasImage, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Image Source="{Binding Image}" Stretch="Uniform"/>
                                </Border>

                                <!-- Информация о кандидате -->
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Name}" FontSize="18" 
                                               FontWeight="Bold" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding Description}" 
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="3" Content="Закрыть" HorizontalAlignment="Right" 
                Padding="20,8" Margin="0,10,0,0" Click="CloseButton_Click"/>
    </Grid>
</Window>
